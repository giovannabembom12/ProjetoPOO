package com.ifam.model.dao;

import java.util.List;

import com.ifam.model.vo.EventModel;

public class EventDAO {
    // Métodos de acesso a dados para Eventos

    /*public boolean saveEvent(EventModel event) {
        // Lógica para salvar o evento no banco de dados
        // Retornar true se o evento for salvo com sucesso, ou false caso contrário
    }

    public boolean updateEvent(EventModel event) {
        // Lógica para atualizar o evento no banco de dados
        // Retornar true se o evento for atualizado com sucesso, ou false caso contrário
    }

    public boolean deleteEvent(int eventId) {
        // Lógica para excluir o evento do banco de dados com base no ID
        // Retornar true se o evento for excluído com sucesso, ou false caso contrário
    }

    public EventModel getEventById(int eventId) {
        // Lógica para recuperar um evento do banco de dados com base no ID
        // Retornar um objeto EventValueObject se encontrado, ou null caso contrário
    }

    public List<EventModel> getAllEvents() {
        // Lógica para recuperar todos os eventos do banco de dados
        // Retornar uma lista de objetos EventValueObject
*/    }

